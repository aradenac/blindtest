doctype html
html(lang="fr")
  head
    script(src="https://code.jquery.com/jquery-3.5.1.min.js")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/underscore.js/1.9.1/underscore-min.js")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/backbone.js/1.4.0/backbone-min.js")
    script(src="https://cdnjs.cloudflare.com/ajax/libs/backbone-localstorage.js/1.1.0/backbone.localStorage-min.js")
    script(src='javascripts/todo.js')

    title Test BackBone
   
    link(rel='stylesheet' type='text/css' href='stylesheets/style.css')
    link(rel='stylesheet' type='text/css' href='stylesheets/todo.css')

  body
    #todoapp
        header
            h1 Todos
            input#new-todo(type='text' placeholder='What needs to be done?')
        section#main
            input#toggle-all(type='checkbox')
            label(for='toggle-all') Mark all as complete
            ul#todo-list
        footer
            a#clear-completed Clear completed
            #todo-count
        #instructions
        | Double-click to edit a todo.
        #credits
        | Created by
        br
        a(href='http://jgn.me/') J&eacute;r&ocirc;me Gravel-Niquet
        | .
        br
        | Rewritten by: 
        a(href='https://github.com/tastejs/todomvc') TodoMVC
        | .

    script#item-template(type='text/template').
        <div class="view">
        <input class="toggle" type="checkbox" <%= done ? 'checked="checked"' : '' %> />
        <label><%- title %></label>
        <a class="destroy"></a>
        </div>
        <input class="edit" type="text" value="<%- title %>" />
    script#stats-template(type='text/template').
        <% if (done) { %>
        <a id="clear-completed">Clear <%= done %> completed <%= done == 1 ? 'item' : 'items' %></a>
        <% } %>
        <div class="todo-count"><b><%= remaining %></b> <%= remaining == 1 ? 'item' : 'items' %> left</div>
    
    